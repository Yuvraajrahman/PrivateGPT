console.log(`
>> PrivateGPT — local dev
>> 1) LM Studio: load your model, then Start Server (OpenAI base http://127.0.0.1:1234/v1)
>> 2) backend/.env + web/.env.local (copy web/.env.local.example if needed; RAG_API_KEY must match)
>> Starting FastAPI :8000 + Next.js…
`);
