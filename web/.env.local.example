# Copy to web/.env.local (gitignored) for local dev with `npm run dev` from repo root.
# RAG_API_KEY must match backend/.env RAG_API_KEY.
RAG_BACKEND_URL=http://127.0.0.1:8000
RAG_API_KEY=change-me-same-as-backend
